[project]
name = "light-web-ui-autotest-demo"
version = "0.1.0"
description = "light-web-ui-tester wheel 冒烟验证用例项目"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
